<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{% block title %}Dentist Back Office{% endblock %}</title>
    <link rel="stylesheet" href="{{ asset('css/styles.css') }}">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css">
</head>
<body>
    <header>
        <nav class="navbar navbar-expand-lg navbar-dark bg-primary">
            <div class="container">
                <a class="navbar-brand" href="{{ path('app_home') }}">Dentist Back Office</a>
                <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
                    <span class="navbar-toggler-icon"></span>
                </button>
                <div class="collapse navbar-collapse" id="navbarNav">
                    <ul class="navbar-nav">
                        <li class="nav-item"><a class="nav-link" href="{{ path('app_secretary_index') }}">Secretaries</a></li>
                        <li class="nav-item"><a class="nav-link" href="{{ path('app_material_index') }}">Materials</a></li>
                        <li class="nav-item"><a class="nav-link" href="{{ path('app_appointment_index') }}">Appointments</a></li>
                        <li class="nav-item"><a class="nav-link" href="{{ path('app_medical_prescription_index') }}">Prescriptions</a></li>
                    </ul>
                </div>
            </div>
        </nav>
    </header>

    <main class="container mt-4">
        {% block body %}{% endblock %}
    </main>

    <footer class="bg-primary text-white text-center py-3 mt-4">
        &copy; {{ 'now'|date('Y') }} Dentist Back Office
    </footer>
</body>
</html>
