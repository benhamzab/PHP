<form method="post" action="{{ path('app_medical_prescription_delete', {'id': medical_prescription.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ medical_prescription.id) }}">
    <button class="btn">Delete</button>
</form>
