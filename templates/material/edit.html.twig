{% extends 'base.html.twig' %}

{% block title %}Edit Material{% endblock %}

{% block body %}
<div class="container mt-5">
    <h1 class="text-center text-primary mb-4">Edit Material</h1>

    <div class="card shadow p-4">
        {{ include('material/_form.html.twig', {'button_label': 'Update'}) }}
    </div>

    <div class="d-flex justify-content-between mt-3">
        <a href="{{ path('app_material_index') }}" class="btn btn-secondary">
            <i class="fas fa-arrow-left"></i> Back to list
        </a>

        {{ include('material/_delete_form.html.twig') }}
    </div>
</div>
{% endblock %}
