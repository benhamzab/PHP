{% extends 'base.html.twig' %}

{% block title %}New Appointment{% endblock %}

{% block body %}
<div class="container mt-5">
    <div class="card shadow-lg p-4">
        <h1 class="text-center text-primary mb-4">Create New Appointment</h1>

        <div class="card-body">
            {{ include('appointment/_form.html.twig', {'button_label': 'Save Appointment'}) }}
        </div>

        <div class="mt-4 text-end">
            <a href="{{ path('app_appointment_index') }}" class="btn btn-secondary">
                <i class="fas fa-arrow-left"></i> Back to List
            </a>
        </div>
    </div>
</div>
{% endblock %}
